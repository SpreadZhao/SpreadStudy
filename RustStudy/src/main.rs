mod guessing_game;
mod ownership;
mod references;
mod slice;
mod structt;
mod variables;

fn main() {
    // guessing_game::guessing_game();
    // variables::variables();
    // ownership::ownership();
    // references::reference();
    slice::slice();
}
